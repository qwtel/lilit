{
  "name": "lilit",
  "version": "0.1.0",
  "description": "",
  "main": "cjs/index.js",
  "scripts": {
    "test": "mocha",
    "install": "npm run build",
    "build": "npm run build:lib && npm run build:cjs",
    "build:lib": "node --experimental-modules --no-warnings scripts/build.mjs",
    "build:cjs": "babel lib --out-dir cjs --plugins transform-es2015-modules-commonjs",
    "format": "prettier --write '{src,test}/*.{js,mjs}'",
    "version": "npm run format && npm test && git add -u"
  },
  "author": "Florian Klampfer <mail@qwtel.com> (https://qwtel.com/)",
  "license": "MIT",
  "engines": {
    "node": ">= 11"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.4",
    "assert": "^1.4.1",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.26.2",
    "mocha": "^6.0.2",
    "prettier": "^1.16.4"
  }
}
