{
  "name": "lilit",
  "version": "0.3.2",
  "description": "",
  "main": "cjs/index.js",
  "module": "mjs/index.mjs",
  "files": [
    "mjs",
    "cjs"
  ],
  "scripts": {
    "test": "npm run build && mocha",
    "build": "npm run build:mjs && npm run build:cjs",
    "build:mjs": "node --experimental-modules --no-warnings scripts/build.mjs",
    "build:cjs": "babel mjs --out-dir cjs",
    "format": "prettier --write '{src,test}/*.{js,mjs}'",
    "preversion": "npm run format && npm test && git add -u"
  },
  "author": "Florian Klampfer <mail@qwtel.com> (https://qwtel.com/)",
  "license": "MIT",
  "engines": {
    "node": ">= 11"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.4",
    "@babel/plugin-transform-modules-commonjs": "^7.2.0",
    "assert": "^1.4.1",
    "mocha": "^6.0.2",
    "prettier": "^1.16.4"
  }
}
